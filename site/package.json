{
  "private": true,
  "scripts": {
    "webpack": "cross-env NODE_ENV=development webpack --progress --hide-modules --env=local",
    "dev": "cross-env NODE_ENV=development webpack --watch --progress --hide-modules --env=local",
    "hmr": "cross-env NODE_ENV=development webpack-dev-server --inline --hot",
    "staging": "cross-env NODE_ENV=staging webpack --progress --hide-modules --env=staging",
    "production": "cross-env NODE_ENV=production webpack --progress --hide-modules --env=production",
    "test": "nyc ava --verbose",
    "posttest": "nyc report --reporter=html"
  },
  "devDependencies": {
    "hasbin": "^1.2.3",
    "laravel-mix": "^0.10.0",
    "webpack-shell-plugin": "^0.5.0",
    "yargs": "^4.6.0"
  }
}
